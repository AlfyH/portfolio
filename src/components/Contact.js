import React, { Component } from 'react';

export default class Contact extends Component {
    render() {
        return (
            <div id = "page-contact">
                <section id="4">
                    Contact
                </section>
            </div>
        )}
}